server:
  port: 5000
  servlet:
    context-path: /notecard

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/local-db?currentSchema=notecard
    username: postgres
    password: g5rEW^
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    show-sql: false
    hibernate:
      ddl-auto: create-drop
management:
  endpoints:
    web:
      exposure:
        include: "*" # temp for development
jwt:
  secret: revature